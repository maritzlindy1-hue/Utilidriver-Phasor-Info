services:
  - type: web
    name: utilidriver-phasor-info
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 1 --timeout 180 app:app
